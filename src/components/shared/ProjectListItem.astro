---
import { Image } from 'astro:assets';
import ImageCaption from '@snowbeat/components/shared/ImageCaption.astro';

interface Props {
    title: string;
    image: ImageMetadata;
    isFeatured?: boolean;
}

const { title, image, isFeatured } = Astro.props;
---

<style>
    article.featured {
        max-width: 1180px;
        margin: 0 auto;
    }

    h2 {
        padding-bottom: 0.5rem;
        font-size: var(--font-size-500);
    }

    .featured h2 {
        padding-bottom: 1.125rem;
        font-size: var(--font-size-600);
    }

    .featured .content-wrapper {
        padding: 16px 20px 20px;
        background-color: #eee;

        @media (--bp-600) {
            margin-inline: 10px;
            border-radius: 12px;
        }

        @media (--bp-800) {
            padding: 30px 40px 40px;
        }
    }

    figure {
        position: relative;
        border-radius: 8px;
        overflow: hidden;
    }

    .main-image {
        width: 100%;
        height: auto;
    }
</style>

<article class={isFeatured ? 'featured' : ''}>
    <div class="content-wrapper">
        <h2>{title}</h2>
        <figure>
            <Image src={image} alt="Tasklane logo" class="main-image" />
            <ImageCaption>Web, iOS, and Android app</ImageCaption>
        </figure>
    </div>
</article>
